{
  "name": "quill-native-miniprogram",
  "version": "2.0.0",
  "private": true,
  "description": "原生微信小程序，无运行时框架和构建依赖",
  "scripts": {
    "check": "node scripts/check.cjs",
    "test": "node --test tests/*.test.cjs",
    "build": "node scripts/check.cjs",
    "build:mp-weixin": "node scripts/check.cjs"
  },
  "engines": {
    "node": ">=18"
  }
}
